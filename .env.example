# Optional: API key for Limelink project authentication (required for API tools)
# LIMELINK_API_KEY=your_api_key_here

# Optional: Default project ID (used when not specified in tool calls)
# LIMELINK_PROJECT_ID=your_project_id_here
